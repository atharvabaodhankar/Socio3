rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Open rules for local testing
    // TODO: Implement proper authentication integration before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}